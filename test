local enabled = settings.enabled

if enabled then
    local codeToExecute = "print('Hello, world!')"
    local success, result = pcall(loadstring(codeToExecute))
    if not success then
        warn("Error running loadstring:", result)
    end
end
